import { NotDirective } from './not.directive';

describe('NotDirective', () => {
  it('should create an instance', () => {
    const directive = new NotDirective();
    expect(directive).toBeTruthy();
  });
});
